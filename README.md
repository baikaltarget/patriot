# Мультихолл — сайт площадки для мероприятий (Парк «Патриот», Иркутск)

Next.js 15 (App Router, статическая генерация) + Tailwind. Хостинг — Vercel через GitHub, бесплатно.
Всё, что нужно знать, чтобы опубликовать сайт и потом править его без программиста, — ниже.

---

## 1. Как опубликовать (10 минут)

1. **GitHub.** Зарегистрируйтесь на github.com → **New repository** → имя `multihall-site`, Private → Create.
   На странице пустого репозитория нажмите **uploading an existing file** и перетащите **содержимое** этой папки
   (не саму папку: в корне репозитория должны лежать `package.json`, `app/`, `content/`).
   Папки `node_modules`, `.next`, `out` не загружать — их нет в архиве. Нажмите **Commit changes**.
2. **Vercel.** vercel.com → Sign up через GitHub → **Add New → Project** → выберите `multihall-site` → **Deploy**.
   Через 1–2 минуты сайт откроется по адресу вида `multihall-site.vercel.app`.
3. **Проверка.** Откройте на телефоне: кнопка «Позвонить» должна набирать номер, меню открываться, калькулятор считать.
4. **Домен.** В Vercel: Project → Settings → Domains → добавить `hall.park-patriot.com`.
   Vercel покажет CNAME-запись `cname.vercel-dns.com` — её добавляют у регистратора домена park-patriot.com (не в Tilda).
   HTTPS включится сам.
5. **Адрес сайта в настройках.** Vercel → Settings → Environment Variables → добавить
   `NEXT_PUBLIC_SITE_URL` = `https://hall.park-patriot.com` → Redeploy. Это нужно для sitemap, canonical и разметки.
6. **Редирект со старой страницы.** На Tilda: Настройки сайта → SEO → Редиректы → `/multi-hall` → `https://hall.park-patriot.com/`, 301.
   В меню парка («Локации → Мультихолл») поменять ссылку на новый адрес.

Если после правки на GitHub сайт не обновился: посмотрите счётчик коммитов на GitHub → Vercel → Deployments (Building / Ready / Error) → Ctrl+Shift+R или инкогнито.

## 2. Заявки в Telegram

Код готов. Нужно завести бота и прописать два ключа в Vercel — примерно 10 минут.

### 2.1. Создать бота

1. В Telegram найти **@BotFather**, нажать Start.
2. Отправить `/newbot`.
3. Ввести имя бота (видно людям), например `Заявки Мультихолл`.
4. Ввести логин, обязательно заканчивается на `bot`, например `multihall_leads_bot`.
5. BotFather пришлёт **токен** вида `8123456789:AAF...` — это `TELEGRAM_BOT_TOKEN`.

### 2.2. Узнать, куда слать заявки

**Вариант А — личные сообщения одному человеку:**
1. Написать своему боту любое сообщение, иначе он не сможет вам писать.
2. Найти **@userinfobot**, нажать Start — он пришлёт ваш `Id`. Это `TELEGRAM_CHAT_ID`.

**Вариант Б — общая группа менеджеров (рекомендуется):**
1. Создать группу, добавить туда бота и всех, кто принимает заявки.
2. Добавить в группу **@getidsbot** — он пришлёт id группы.
3. Id группы начинается с минуса, например `-1001234567890`. Минус обязателен.
4. Бота в группе лучше сделать администратором.

Можно указать **несколько получателей через запятую**: `123456789,-1001234567890`.
Сообщение уйдёт всем; заявка считается доставленной, если дошла хотя бы до одного.

### 2.3. Прописать ключи в Vercel

1. Vercel → проект → **Settings → Environment Variables**.
2. Добавить две переменные для окружения Production:
   - `TELEGRAM_BOT_TOKEN` — токен от BotFather
   - `TELEGRAM_CHAT_ID` — id получателя или несколько через запятую
3. **Deployments → последний деплой → Redeploy.** Без пересборки переменные не подхватятся.

### 2.4. Проверить

Отправить тестовую заявку с сайта. Придёт сообщение с кликабельным телефоном,
форматом мероприятия, числом гостей, временем по Иркутску и ссылкой на страницу,
с которой оставили заявку. Из калькулятора дополнительно приходит сумма расчёта.

Если не пришло — Vercel → Deployments → Functions → Logs, искать строки `[lead]`:

| Ошибка в логе | Что значит |
|---|---|
| `400 chat not found` | Неверный id, либо боту не написали первым, либо бот не в группе |
| `401 Unauthorized` | Неверный токен |
| `403 bot was blocked` | Получатель заблокировал бота |

**Заявка пишется в лог всегда**, даже если Telegram недоступен, — ни одна не потеряется.

### 2.5. Кнопка «Написать в Telegram»

На формах есть запасная кнопка, открывающая Telegram с готовым текстом заявки.
Она появляется, только если заполнить `content/site.json` → `site.telegram`
(логин без `@`). Это аккаунт менеджера для переписки, не тот, что принимает заявки.

## 3. Дизайн: две версии, переключаются одной строкой

Сейчас включена **версия 3 — «карточки»**: hero на фото с тёмной подложкой, крупные цифры в серых карточках, фото-карточки залов с ценой, кнопки-пилюли, скругления 24 px. Цвета — те же, что на park-patriot.com: красный `#DB2200`, синий `#0D70CC`, футер `#1C1C1C`, логотип — векторный из файлов парка.

**Версия 2 — «квадратная, как у парка»** (без скруглений, плоские фото сбоку, прямые кнопки) сохранена в архиве `design-rollback-v2-park-style.zip`.

### Быстрый переключатель без отката

В `app/globals.css` в `:root` есть переменная `--radius: 24px`. Поставьте `--radius: 0px; --radius-sm: 0px; --radius-pill: 0px;` — все карточки, кнопки и фото станут квадратными, как на сайте парка, при той же структуре. Это самый дешёвый способ показать клиенту оба варианта.

### Полный откат на версию 2

1. Распаковать `design-rollback-v2-park-style.zip`.
2. На GitHub перетащить его содержимое в корень репозитория **поверх** текущих файлов (это те же 12 файлов дизайна, они просто перезапишутся) → Commit.
3. Ничего удалять не нужно: между версиями 2 и 3 файлы не удалялись, только менялись.

Контент (`content/site.json`, статьи, фото) при любом переключении не трогается.

## 4. Живые фото — что есть и откуда

Все фото на сайте — настоящие, из архива заказчика (Save As park-patriot.com + отдельная подборка фото мероприятий), кроме размеченных красной рамкой.

| Файл | Что на фото | Где используется |
|---|---|---|
| `hall-01.jpg` | Пустой зал, рассадка театром | Главная, hero |
| `hall-02.jpg` | Полный зал сверху, вид на сцену | Галереи |
| `hall-03.jpg` | Зал во время мероприятия, экран «9 мая» | Галереи, конференц-зал |
| `conf-01.jpg` | Панельная дискуссия «Легенды Роскосмоса» | О площадке, конференц-зал |
| `conf-02.jpg` | Ещё одна панельная дискуссия | Конференц-зал, презентация |
| `format-class.jpg` | Шахматный турнир, рассадка классом рядами столов | Семинар, тренинг, мастер-класс — буквальное совпадение с форматом |
| `koncert-01.jpg` | Награждение, цветная подсветка сцены | Корпоратив, концерты |
| `koncert-02.jpg` | Брендированная сцена с партнёрскими логотипами (Роскосмос, VID Media) | Новогодний, площадка, выставка |
| `vks-01.jpg` | Видеоконференцсвязь в работе на мероприятии | Оборудование, конференц-зал |
| `festival-01.jpg` | Детский фестиваль на сцене | О площадке, площадка |
| `kids-01.jpg`, `kids-02.jpg` | Детские праздники (утренник, фотозона) | О площадке |

**Не использовано:** фото с музейными стендами и ящиками из-под боеприпасов (`IMG_20210509_122409`) — по кирпичной колонне и витринам видно, что это другое помещение парка, не Мультихолл. Исключено, чтобы не выдавать чужой интерьер за зал.

**Официальные схемы рассадки** (`scheme-theatre.jpg`, `scheme-class.jpg`, `scheme-banquet-pt.jpg`, `scheme-banquet-round.jpg`) — растровые изображения из файлов сайта, не мой рисунок. Три из них — маленькие (226×160 px), что видно по чуть смазанному отображению на странице зала; выглядят нормально в текущем размере карточки, но если появятся файлы большего разрешения — просто заменить по тем же именам в `public/img/`.

**Ещё не найдены:** фото стрелкового клуба и фаертага (`park-shooting.jpg`, `park-firetag.jpg` — сейчас плейсхолдеры на странице «Корпоратив»), отдельные фото малого зала и переговорной (сейчас плейсхолдеры на `/zaly/malyy-zal/` и `/zaly/peregovornaya/`).

## 5. Источник цифр вместимости — важно прочитать

Первая версия сайта использовала вместимость «до 300 человек» из брифа с семантикой. В присланных файлах сайта нашлись **официальный поэтажный план** и **официальные схемы рассадки** — это первичный источник точнее брифа, поэтому сайт пересчитан на них:

| | Было в брифе | Стало (по официальным документам) |
|---|---|---|
| Площадь зала | 346 м² | 300 м² (299,61 м² по обмерам) |
| Театр | до 300 | **175** |
| Класс | до 100 | **70** (компактно — 66) |
| Банкет | до 200 | **96** за круглыми столами (компактно у стен — 36–40) |
| Малый зал | 68 м² | 68 м² — подтвердилось |
| Переговорная | 46 м² | 46 м² — подтвердилось |

Если у заказчика есть более новые официальные цифры — поправить в `content/site.json` → `hall.capacityMax` и `hall.layouts[].capacity`, дальше все страницы, FAQ и JSON-LD пересчитаются сами при следующей сборке (они берут числа оттуда же).

## 6. Два вопроса заказчику до сдачи

1. **Комната №8 на плане, 34,63 м².** Есть на официальном поэтажном плане, не описана в брифе. Что это — второй малый зал, гардероб, техническое помещение? Пока не выносил на сайт — если это ещё одно сдаваемое помещение, стоит добавить как четвёртый зал.
2. **Детские мероприятия.** В присланных фото — минимум два детских праздника (`0001`, `0003`, `005`). Это разовые прошлые заказы или значимое направление? Если второе — имеет смысл кластер запросов «детский праздник в Иркутске», «выпускной», «утренник» и отдельная страница; проверить частотность так же, как остальные кластеры. Пока это только фон на «О площадке», без отдельной посадочной.

## 7. Где что править

| Что | Файл |
|---|---|
| Телефон, адрес, часы, реквизиты, соцсети, логотипы | `content/site.json` → `site` |
| Площадь, вместимость, схемы рассадки | `content/site.json` → `hall` |
| Тарифы, доп. оборудование, цены малых залов, условия | `content/site.json` → `pricing` |
| Тексты страниц, FAQ, блоки | `content/site.json` → `pages` |
| Прайс оборудования | `content/site.json` → `equipment` |
| Даты декабря на странице новогоднего | `content/site.json` → `pages.newYear.dates` |
| Статьи блога | `content/blog/*.md` (заголовок и описание в шапке файла) |
| Фото | `public/img/` |
| Логотипы | `public/logo/` |
| Цвета, шрифты, радиус углов | `tailwind.config.ts`, `app/globals.css` |
| Меню | `content/site.json` → `nav` |

Правка на GitHub: открыть файл → карандаш → изменить → **Commit changes**. Vercel пересоберёт сайт сам.
JSON строгий: кавычки двойные, после последнего элемента в списке запятой нет. Если сайт не собрался — Vercel покажет Error, проверьте запятые.

### Добавить страницу

- **Зал** — скопировать блок в `halls[]`, задать новый `slug`. Страница `/zaly/<slug>/` появится сама, попадёт в sitemap и в список залов.
- **Формат мероприятия** — блок в `formats[]` → `/format/<slug>/`. Если добавляете формат, укажите фото в `formatImage` внутри `app/format/[slug]/page.tsx` — иначе подставится общее фото зала.
- **Проведённое мероприятие со сметой** — блок в `cases[]` → `/meropriyatiya/<slug>/`. Смета считается автоматически по `pricing` из полей `duration` (`short` / `day` / `twodays`), `extras` (`mic`, `sound`, `projector`), `rooms` (`small`, `meeting`) и `roomsHours`. **Не превышайте реальную вместимость** (175 / 70 / 96 в зависимости от `layout`) — иначе кейс сам себе противоречит.
- **Статья** — новый файл `content/blog/имя-латиницей.md` по образцу существующих.

## 8. Красные рамки — что подтвердить у заказчика

Пунктирная рамка (фиолетовая, `needsData`) — везде, где данные не подтверждены. Список:

- **5 мероприятий в `cases[]`** — правдоподобные примеры, не реальные кейсы. Заменить на настоящие: название, формат, гостей (в пределах реальной вместимости), что арендовали.
- **Цены малого зала (1 500 ₽/ч) и переговорной (1 000 ₽/ч)** — `pricing.roomsHourly`.
- **Условия**: безнал, предоплата 30 %, свой кейтеринг и алкоголь без сбора, парковка — `pricing.terms` и FAQ.
- **Вместимость фуршета (до 150)** — оценка по площади зала, официальной схемы для этого формата нет.
- **Даты декабря** — `pages.newYear.dates`.
- **Комната №8, 34,63 м²** — см. раздел 6.
- **Координаты** `site.geo` — приблизительные, для карты и разметки LocalBusiness нужны точные.
- **Карта** на странице контактов — вставить код из Конструктора карт Яндекса в `app/kontakty/page.tsx` вместо серого блока.
- **Проезд** на контактах — остановки транспорта, парковка.
- **Wi-Fi, естественный свет, ширина въезда, ранний монтаж** — отдельные ответы в FAQ.
- **Текст «О площадке»** и **политика конфиденциальности** — черновики.
- **Реквизиты**: на сайте парка два юрлица встречаются (АНО и ООО). Указать то, с которым заключается договор на аренду зала.
- **Фото**: стрелковый клуб, фаертаг, малый зал, переговорная — см. раздел 4.

## 9. Структура адресов

```
/                              главная — аренда зала для мероприятий
/konferenc-zal/                конференц-зал
/korporativ/                   корпоратив и тимбилдинг
/korporativ/novogodniy/        новогодний корпоратив (сезонная, менять год в title)
/ploshchadka/                  концерты, выставки, съёмки
/oborudovanie/                 оборудование и райдер
/ceny/                         цены + калькулятор
/zaly/  /zaly/<slug>/          залы: multihall, malyy-zal, peregovornaya
/format/<slug>/                seminar, trening, prezentaciya, master-klass, vystavka, koncert
/meropriyatiya/  /<slug>/      проведённые мероприятия со сметой
/blog/  /blog/<slug>/          статьи
/o-ploshchadke/  /kontakty/  /politika/
/sitemap.xml  /robots.txt      генерируются сами
/api/lead                      приём заявок
```

## Цели Яндекс.Метрики

Счётчик 112694992 стоит на всех страницах: вебвизор, карта кликов, точный показатель отказов.
Сайт отправляет события — их нужно один раз завести как цели:
**Метрика → Настройка → Цели → Добавить цель → тип «JavaScript-событие»** → вставить идентификатор.

| Идентификатор | Что считает | Зачем |
|---|---|---|
| `lead_form_submit` | Отправлена форма заявки | Главная цель, по ней стоимость заявки |
| `calc_lead_submit` | Отправлен расчёт из калькулятора с телефоном | Вторая главная, самый тёплый лид |
| `calc_finish` | Дошёл до шага «Расчёт», но телефон не оставил | Показывает интерес к цене без заявки |
| `phone_click` | Клик по номеру телефона | Часть заявок идёт звонком |
| `email_click` | Клик по почте | Обычно юрлица |
| `telegram_click` | Клик по кнопке Telegram | Заработает после заполнения `site.telegram` |
| `map_open` | Открыл карту на контактах | Косвенный признак намерения приехать |

`lead_form_submit` и `calc_lead_submit` передают параметры (формат, число гостей, сумма расчёта) —
смотреть в отчёте «Параметры визитов», так видно, какие форматы приносят заявки.

Проверить, что цели срабатывают: добавить к адресу страницы `?_ym_debug=1` и смотреть консоль браузера.

Ещё стоит связать счётчик с Вебмастером: Вебмастер → Настройки → Привязка Метрики.

## Подтверждение прав в поисковиках

- **Яндекс.Вебмастер** — метатегом, код в `content/site.json` → `site.verification.yandex`.
  В Вебмастере выбрать способ «Метатег» → «Подтвердить».
- **Google Search Console** — файлом `public/google7e44d841e063b6ab.html`. Ресурс должен быть добавлен
  как «Ресурс с префиксом URL» (`https://hall.park-patriot.com`), способ проверки — «Файл HTML».

## 10. После публикации

1. **Яндекс Вебмастер**: добавить `hall.park-patriot.com` как отдельный сайт, подтвердить, указать регион **Иркутск**, отправить `sitemap.xml`.
2. **Google Search Console**: то же, отправить sitemap.
3. **Яндекс Метрика**: создать счётчик, код вставить в `app/layout.tsx` перед `</body>` (там есть комментарий). Цели: отправка формы, клик по телефону.
4. **Яндекс Бизнес и 2ГИС**: отдельная карточка «Мультихолл» с категорией «Площадка для мероприятий / Конференц-зал», телефон и адрес — как на сайте до символа.
5. **Ссылка с сайта парка** — из меню и футера на новый домен.

## 11. Локальный запуск (для разработчика)

```
npm install
npm run dev      # http://localhost:3000
npm run build    # проверка сборки
```

Шрифт Golos Text лежит в `app/fonts/` и подключён локально — внешние запросы к Google Fonts не нужны. Логотипы — векторные SVG в `public/logo/`, фото — JPEG в `public/img/`, все пересжаты до ширины 1600 px.

## Честные ожидания

Индексация — 2–4 недели. «Переговорная», «зал для семинара», «где провести корпоратив» — топ-10 через 2–3 месяца.
«Конференц зал иркутск» — там гостиницы с возрастом домена, полгода и без гарантий.
Реальная вместимость (175 театром, а не «до 300») — не помешает продвижению: длинные запросы вроде «зал на 150 человек иркутск» она как раз закрывает точнее.
